{
  "name": "simple-confluence-to-markdown",
  "version": "2.0.0",
  "author": "nianqin",
  "license": "MIT",
  "description": "Convert Confluence Pages to Markdown",
  "keywords": [
    "Confluence",
    "Markdown",
    "Confluence to Markdown"
  ],
  "bin": "./bin/confluence-to-markdown",
  "main": "./src/index.coffee",
  "repository": "https://github.com/nqdy666/simple-confluence-to-markdown.git",
  "dependencies": {
    "cheerio": "^0.22.0",
    "coffee-script": "^1.10.0",
    "fs": "0.0.1-security",
    "mkdirp": "^0.5.1",
    "ncp": "^2.0.0",
    "path": "^0.12.7",
    "rimraf": "^2.5.4",
    "sync-exec": "^0.6.2"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "cross-env": "^7.0.3",
    "mocha": "^3.1.0"
  },
  "scripts": {
    "start": "coffee ./src/index.coffee",
    "build": "coffee --output dist --compile src",
    "test": "cross-env NODE_ENV=test mocha -r coffee-script/register --compilers coffee:coffee-script --recursive ./test/*.coffee",
    "test-watch": "cross-env NODE_ENV=test mocha -r coffee-script/register --compilers coffee:coffee-script --recursive test/*.coffee --watch"
  }
}
